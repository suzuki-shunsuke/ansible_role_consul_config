{
  "version": "0.4.0",
  "scripts": {
    "release": "standard-version",
    "commitmsg": "commitlint -e $GIT_PARAMS",
    "test": "bash tests/test_docker.sh"
  },
  "devDependencies": {
    "@commitlint/cli": "^7.0.0",
    "@commitlint/config-angular": "^7.0.1",
    "@commitlint/travis-cli": "^7.0.0",
    "husky": "^0.14.3",
    "standard-version": "^4.2.0"
  }
}
